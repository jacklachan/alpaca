# Handover

The current handover is [`HANDOVER-2.md`](HANDOVER-2.md). It supersedes all
earlier architecture and status notes.

The critical boundary is unchanged: do not place an order, deploy, push, or
merge without the required external input and explicit user direction. The
scored path is options-only, and bounded AI may select one deterministic,
pre-priced candidate or abstain.
